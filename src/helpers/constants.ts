import path from 'node:path';

export const avatarStoragePath = path.join(__dirname, '../', '../', 'public', 'avatars');
export const tempStoragePath = path.join(__dirname, '../', '../', 'temp', 'avatars');
